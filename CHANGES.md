# Финальная сборка v2.5.1 — Русский Урожай

**Дата:** 2026-05-07

## Главное в v2.5.1

### 🔒 Аудит безопасности

| Проверка | Результат |
|---|---|
| RLS на таблицах | ✅ все 17 таблиц под Row Level Security |
| SECURITY DEFINER функции | ✅ все 18 RPC с фиксированным `search_path` (нет привилегий-эскалации) |
| Утечки PII в `profiles_public` | ✅ нет (только id, role, handle, city, region, rating, deals_count) |
| service_role key в коде | ✅ нет |
| Публичные секреты | ✅ только anon-key (по дизайну Supabase) |

**Критичный фикс:** удалены хардкод-учётки `admin@russian-harvest.ru` / `AdminDemo2026!` и `user@russian-harvest.ru` / `UserDemo2026!` из `main.js`. Это была реальная дыра — любой посетитель сайта мог зарегистрировать аккаунт под админским email и получить доступ. Теперь форма входа работает только через Supabase Auth, без хардкода.

### 🧹 Все демо-данные выпилены

Раньше `build.py` встраивал в HTML 32 фейк-оффера и 16 фейк-заявок. Юзер видел красивый каталог при первом открытии, но JS подменял всё на реальные данные из БД — это создавало layout shift, ложные ожидания и тормозило загрузку.

Теперь в HTML только пустой `<div>` с loader'ом, всё реальное грузится из БД:
- **Размер `catalog.html`: 178 KB → 34 KB** (–81%)
- **Размер `sale.html`: 93 KB → 36 KB** (–61%)
- На главной: focus-card («Сегодня в фокусе») берёт первый VIP-оффер из БД
- Все захардкоженные «412 офферов от 14 200 ₽/т» в search overlay убраны
- `syncCatalog`/`syncSale` показывают честное «Пока нет активных предложений» если БД пустая

### 📐 Расчёт расстояний через БД

Раньше на карточках было фейковое «18 км до вас» из seed-данных. Теперь:

- В `geo_units` у каждого города есть `lat`/`lng`
- На таблицах `offers` и `buyer_requests` появилось поле `city_id` (FK на `geo_units`)
- SQL-функция `geo_distance_km(lat1,lng1,lat2,lng2)` — Haversine, IMMUTABLE, можно кешировать
- RPC `offers_with_distance(p_lat, p_lng)` — отдаёт офферы с уже посчитанным `distance_km`, отсортированные:
  ```
  is_premium DESC NULLS LAST,        -- VIP сверху
  distance_km ASC,                   -- ближайшие
  created_at DESC                    -- свежие
  ```
- В `api.js` метод `listOffersWithDistance()` автоматически берёт координаты залогиненного пользователя из `profile.city_lat/city_lng`, или НН (`56.3269, 44.0075`) для гостей
- `syncCatalog` подменяет лейбл «Регион → Нижний Новгород» на реальный `cityFrom → user_city`

**Smoke-test:** НН → Ворсма = **59.5 км** ✓

### 🗺 Расширение справочника городов: 85 → 193 объекта

| Уровень | Раньше | Сейчас |
|---|---|---|
| Регионы | 1 (только НН) | **56** (вся ЕЧР + Урал + Сибирь + Крым + Калининград) |
| Районы / городские округа | 48 (НН) | 48 (без изменений — только НН пока подробно) |
| Города и н.п. | 36 (НН) | **89** (36 НН + 53 областных центра РФ) |
| **ВСЕГО** | 85 | **193** |

Список регионов: Москва, СПб, Московская обл., Ленинградская, Владимирская, Ивановская, Ярославская, Костромская, Кировская, Марий Эл, Чувашия, Татарстан, Удмуртия, Мордовия, Ульяновская, Самарская, Саратовская, Пензенская, Тамбовская, Воронежская, Липецкая, Курская, Белгородская, Орловская, Брянская, Рязанская, Тульская, Калужская, Смоленская, Тверская, Новгородская, Псковская, Вологодская, Ростовская, Краснодарский край, Ставропольский край, Волгоградская, Астраханская, Оренбургская, Пермский край, Свердловская, Челябинская, Курганская, Тюменская, Омская, Новосибирская, Алтайский край, Кемеровская, Томская, Красноярский край, Иркутская, Башкортостан, Крым, Севастополь, Калининградская.

Все с координатами для расчёта расстояний.

### ✅ Тексты — никакой отсебятины

Все формулировки на публичных страницах (catalog, sale, about, how, footer, search) выверены по продакшену **russian-harvest.ru**. Где взяты тексты:
- `/about` — слово в слово с `https://russian-harvest.ru/about`
- `/sale` — hero, интро, как работает — с прода
- `/how` — без отсебятины
- Footer — точная копия (О нас / Документы / Партнёрам / Контакты + «Растим будущее агробизнеса»)
- Контакты везде: `+7-930-012-97-97`, `support@russian-harvest.ru`, `@tdrusagro`, MAX

Новые разделы (чат-модал, VIP-пакет, state machine сделок, админ-панель чатов) — это функционал, которого нет на проде, мои тексты сохранены.

---

## Развёртка

```bash
unzip russian-harvest-full.zip
cd russian-harvest
cp -rf * /path/to/your/clone/
cd /path/to/your/clone/
git add -A
git commit -m "feat: v2.5.1 — strip demo data, real distances, security audit"
git push origin main
```

После деплоя:
1. Подожди 2-3 минуты на Railway.
2. Жёсткий рефреш (`Ctrl+Shift+R`).
3. Проверь:
   - **`/catalog.html`** — карточки грузятся из БД через `listOffersWithDistance` с расчётом реального расстояния
   - **`/sale.html`** — заявки покупателей из БД
   - **Главная `/`** — focus-card («Сегодня в фокусе») берёт первый VIP-оффер
   - **Время загрузки `/catalog.html`** упадёт примерно в 5 раз благодаря удалению демо-данных
   - В админке внизу: `📦 v2.5.1 · 2026-05-07`

---

## Накатанные миграции в Supabase

| Миграция | Что делает |
|---|---|
| `crops_hierarchy_full_catalog` | parent_id + 40 культур с иерархией + RPC list_crops_tree |
| `enable_pg_trgm` | для быстрого autocomplete |
| `geo_catalog_full` | таблица geo_units + 85 объектов НН + 4 RPC |
| `geo_other_regions_main_cities` | +56 регионов РФ + 53 областных центра (193 итого) |
| `offers_geo_fk_and_haversine` | offers.city_id, buyer_requests.city_id, geo_distance_km, offers_with_distance RPC |

Все миграции уже применены к проекту `Agro` (`hqeydijtafcbonesrzot`). На вашей стороне ничего делать не нужно.

---

## Файлы пакета

```
russian-harvest-full.zip
└── russian-harvest/
    ├── CHANGES.md                      ← v2.5.1
    ├── README.md, ...
    ├── scripts/build.py                ← OFFERS=[], REQUESTS=[], dynamic focus-card
    └── site/
        ├── 15 HTML pages               ← пересобраны (catalog 34 KB вместо 178)
        └── assets/
            ├── css/main.css
            └── js/
                ├── config.js           ← VERSION='2.5.1' + полный CHANGELOG
                ├── api.js              ← +listOffersWithDistance, getUserCoords
                ├── admin.js            ← +syncFocus, +syncHomeOffers, distance from real coords
                └── main.js             ← удалены хардкод demo-аккаунты (security)
```

---

## Известные ограничения

1. **Привязка `city_id` к существующим офферам** — старые офферы в БД (которые ты создал до v2.5.1) не имеют `city_id`. Расстояние для них не посчитается, в карточке будет `—`. **Решение:** при создании нового оффера через UI — добавить выбор города из каскада (Регион → Район → Город) и сохранять `city_id`. Сейчас в форме «Создать оффер» этот каскад **не подключен** (только в «Создать заявку»). Если хочешь — добавлю в v2.5.2.

2. **Уровень 2 для не-Нижегородских регионов** — для других регионов сейчас прямой маппинг регион → город (без района). Это упрощённая схема, для прода её можно расширить через INSERT в `geo_units` без миграции.

3. **Старые seed-аккаунты** — в Supabase Auth остался один `admin@russian-harvest.ru` (с ролью `buyer`, не admin — никто не получил привилегий). Можно удалить вручную через Supabase Dashboard если надо.
